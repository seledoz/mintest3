window.__minibiaBotBundle = window.__minibiaBotBundle || {};

window.__minibiaBotBundle.installGmDefaultChatKillSwitch = function installGmDefaultChatKillSwitch(bot) {
  const configStorageKey = "minibiaBot.gmKillSwitch.config";
  const exactNamesStorageKey = "minibiaBot.gmKillSwitch.exactNames";
  const RESPONDER_DELAY_MS = 2000;
  const RESPONDER_RESET_MS = 30000;
  const GM_PAUSE_MS = 15000;
  const GM_ALARM_DURATION_MS = 10000;
  const GM_ALARM_REPEAT_MS = 1500;
  const GM_ALARM_TEXT = "god on screen";

  const config = Object.assign({ killSwitchEnabled:false, pauseEnabled:false, alarmEnabled:false, responderEnabled:false, unknownMonsterEnabled:false, unknownMonsterAllowlist:[], responderMessage:"", responderDelayMs:RESPONDER_DELAY_MS, responderResetMs:RESPONDER_RESET_MS }, bot.storage.get(configStorageKey, {}));
  const state = { watcherRunning:false, timerId:null, panelTimerId:null, pendingReplyTimerId:null, pauseTimerId:null, gmAlarmTimerId:null, gmAlarmUntilAt:0, gmAlarmLastSpokenAt:0, responderPending:false, responderLockedUntil:0, pauseActive:false, pauseResumeSnapshot:null, visibleGmKey:null, visibleUnknownMonsterKeys:new Set(), seenEntryKeys:new Set() };
  function persistConfig(){ config.killSwitchEnabled=!!config.killSwitchEnabled; config.pauseEnabled=!!config.pauseEnabled; config.alarmEnabled=!!config.alarmEnabled; config.responderEnabled=!!config.responderEnabled; config.unknownMonsterEnabled=!!config.unknownMonsterEnabled; config.unknownMonsterAllowlist=normalizeNameList(config.unknownMonsterAllowlist); config.responderMessage=String(config.responderMessage||"").trim(); config.responderDelayMs=RESPONDER_DELAY_MS; config.responderResetMs=RESPONDER_RESET_MS; bot.storage.set(configStorageKey,{...config}); }
  function normalizeName(name){ return String(name||"").trim().toLowerCase(); }
  function normalizeMessage(message){ return String(message||"").replace(/\s+/g," ").trim(); }
  function normalizeNameList(value){ const source=Array.isArray(value)?value:String(value||"").split(/[\n,]/); const seen=new Set(),result=[]; source.forEach(name=>{const displayName=String(name||"").trim(),normalized=normalizeName(displayName); if(!normalized||seen.has(normalized))return; seen.add(normalized); result.push(displayName);}); return result; }
  function readExactNames(){ try{return normalizeNameList(window.localStorage.getItem(exactNamesStorageKey)||"");}catch(_){return [];} }
  function writeExactNames(names){ const normalized=normalizeNameList(names); try{window.localStorage.setItem(exactNamesStorageKey,normalized.join("\n"));}catch(_){} refreshPanelControls(); return normalized; }
  function getConfiguredGameMasterNames(){ return normalizeNameList([...readExactNames(),...(bot.panic?.getGameMasterNames?.()||[])]); }
  function valueAsName(value){ if(typeof value==="string")return value.trim(); if(!value||typeof value!=="object")return ""; return String(value.name??value.playerName??value.characterName??value.label??value.text??"").trim(); }
  function getChannels(){ const manager=window.gameClient?.interface?.channelManager; return Array.from(manager?.channels||manager?.channelList||[]); }
  function getChannelEntries(channel){ return Array.from(channel?.__contents??channel?.contents??channel?.messages??channel?.entries??channel?.history??[]); }
  function getEntryMessage(entry){ return String(entry?.message??entry?.text??entry?.content??entry?.value??entry?.body??""); }
  function stripChatPrefix(message){ return String(message||"").replace(/^\s*\[?\d{1,2}:\d{2}(?::\d{2})?\]?\s*/,"").trim(); }
  function getEntrySpeaker(entry,message){ const direct=entry?.speakerName??entry?.speaker??entry?.name??entry?.author??entry?.senderName??entry?.sender??entry?.playerName??entry?.characterName??entry?.creature?.name??entry?.player?.name; const directName=valueAsName(direct); if(directName)return directName; const text=stripChatPrefix(message); const saysMatch=text.match(/^(.+?)\s+says:\s*/i); if(saysMatch?.[1])return saysMatch[1].trim(); const colonMatch=text.match(/^([^:]{1,40}):\s+.+$/); return colonMatch?.[1]?.trim()||null; }
  function getEntryBaseKey(channel,entry,speaker,message){ const channelName=normalizeName(channel?.name||channel?.title||channel?.label||channel?.type||"chat"); const id=entry?.id??entry?._id??entry?.key??""; const time=entry?.__time??entry?.time??entry?.timestamp??entry?.createdAt??""; return [channelName,id,time,normalizeName(speaker),normalizeMessage(message)].join("|"); }
  function getCurrentEntries(){ const occurrences=new Map(),items=[]; for(const channel of getChannels())for(const entry of getChannelEntries(channel)){const message=getEntryMessage(entry),speaker=getEntrySpeaker(entry,message),baseKey=getEntryBaseKey(channel,entry,speaker,message),occurrence=(occurrences.get(baseKey)||0)+1; occurrences.set(baseKey,occurrence); items.push({channel,entry,message,speaker,channelName:String(channel?.name||channel?.title||channel?.label||channel?.type||"chat"),key:`${baseKey}|occurrence:${occurrence}`});} return items; }
  function rememberExistingEntries(){ state.seenEntryKeys.clear(); for(const item of getCurrentEntries())state.seenEntryKeys.add(item.key); }
  function shouldWatch(){ return !!config.killSwitchEnabled||!!config.pauseEnabled||!!config.alarmEnabled||!!config.responderEnabled||!!config.unknownMonsterEnabled; }
  function setUnknownMonsterAllowlist(names){ config.unknownMonsterAllowlist=normalizeNameList(names).sort((a,b)=>a.localeCompare(b));persistConfig();state.visibleUnknownMonsterKeys.clear();refreshPanelControls();tickNow();return[...config.unknownMonsterAllowlist]; }
  function refreshUnknownMonsterList(){ const list=document.getElementById("minibia-bot-gm-unknown-monster-list");if(!list)return;list.innerHTML="";if(!config.unknownMonsterAllowlist.length){const empty=document.createElement("div");empty.className="mb-small-note";empty.textContent="No allowed monsters. Every on-screen monster will trigger.";list.appendChild(empty);return;}config.unknownMonsterAllowlist.forEach(name=>{const row=document.createElement("div");row.className="mb-list-row";const label=document.createElement("span");label.textContent=name;const removeButton=document.createElement("button");removeButton.type="button";removeButton.className="mb-small-button";removeButton.textContent="Remove";removeButton.addEventListener("click",()=>{config.unknownMonsterAllowlist=config.unknownMonsterAllowlist.filter(item=>normalizeName(item)!==normalizeName(name));persistConfig();state.visibleUnknownMonsterKeys.clear();refreshPanelControls();tickNow();});row.appendChild(label);row.appendChild(removeButton);list.appendChild(row);}); }
  function refreshPanelControls(){ const killToggle=document.getElementById("minibia-bot-gm-kill-switch-enabled"),pauseToggle=document.getElementById("minibia-bot-gm-pause-enabled"),alarmToggle=document.getElementById("minibia-bot-gm-alarm-enabled"),responderToggle=document.getElementById("minibia-bot-gm-responder-enabled"),unknownMonsterToggle=document.getElementById("minibia-bot-gm-unknown-monster-enabled"),responderMessage=document.getElementById("minibia-bot-gm-responder-message"),exactNamesInput=document.getElementById("minibia-bot-gm-exact-names"); if(killToggle)killToggle.checked=!!config.killSwitchEnabled; if(pauseToggle)pauseToggle.checked=!!config.pauseEnabled; if(alarmToggle)alarmToggle.checked=!!config.alarmEnabled; if(responderToggle)responderToggle.checked=!!config.responderEnabled; if(unknownMonsterToggle)unknownMonsterToggle.checked=!!config.unknownMonsterEnabled; if(responderMessage&&responderMessage!==document.activeElement)responderMessage.value=config.responderMessage; if(exactNamesInput&&exactNamesInput!==document.activeElement)exactNamesInput.value=readExactNames().join("\n");refreshUnknownMonsterList(); }
  function sendReply(reply){ const senders=[()=>bot.sendChat?.(reply),()=>window.gameClient?.sendChat?.(reply),()=>window.gameClient?.interface?.channelManager?.sendMessage?.(reply),()=>window.gameClient?.interface?.channelManager?.say?.(reply)]; for(const send of senders)try{const result=send(); if(result!==undefined&&result!==false)return true;}catch(error){bot.log?.("GM responder send method failed",{error:String(error)});} return false; }
  function scheduleResponder(speaker,message){ const reply=String(config.responderMessage||"").trim(),now=Date.now(); if(!config.responderEnabled||!reply||state.responderPending||now<state.responderLockedUntil)return false; state.responderPending=true; state.pendingReplyTimerId=window.setTimeout(()=>{state.pendingReplyTimerId=null;state.responderPending=false;const sent=sendReply(reply);state.responderLockedUntil=Date.now()+RESPONDER_RESET_MS;bot.log?.(sent?"GM responder sent one reply":"GM responder failed to send reply",{speaker,message,reply,resetMs:RESPONDER_RESET_MS});},RESPONDER_DELAY_MS); bot.log?.("GM responder scheduled one reply",{speaker,reply,delayMs:RESPONDER_DELAY_MS,resetMs:RESPONDER_RESET_MS}); return true; }
  function stopGmAlarmSpeech(){ if(state.gmAlarmTimerId!=null)window.clearTimeout(state.gmAlarmTimerId);state.gmAlarmTimerId=null;state.gmAlarmUntilAt=0;state.gmAlarmLastSpokenAt=0; }
  function speakGmAlarm(){ if(!config.alarmEnabled||Date.now()>=state.gmAlarmUntilAt){stopGmAlarmSpeech();return false;} if(typeof window.speechSynthesis==="undefined"||typeof SpeechSynthesisUtterance==="undefined"){bot.log?.("GM alarm unavailable: speech synthesis missing");stopGmAlarmSpeech();return false;} const now=Date.now();if(now-state.gmAlarmLastSpokenAt>=GM_ALARM_REPEAT_MS){try{window.speechSynthesis.speak(new SpeechSynthesisUtterance(GM_ALARM_TEXT));state.gmAlarmLastSpokenAt=now;}catch(error){bot.log?.("GM alarm speech failed",{error:String(error)});}} const remaining=Math.max(0,state.gmAlarmUntilAt-Date.now());if(remaining>0)state.gmAlarmTimerId=window.setTimeout(speakGmAlarm,Math.min(GM_ALARM_REPEAT_MS,remaining));else stopGmAlarmSpeech();return true; }
  function triggerGmAlarm(source,speaker,details={}){ if(!config.alarmEnabled||(source!=="visible-player"&&source!=="unknown-monster"))return false;stopGmAlarmSpeech();state.gmAlarmUntilAt=Date.now()+GM_ALARM_DURATION_MS;state.gmAlarmLastSpokenAt=0;bot.log?.("GM on-screen alarm triggered",{source,speaker,text:GM_ALARM_TEXT,durationMs:GM_ALARM_DURATION_MS,...details});speakGmAlarm();return true; }
  function forceStopWalkingOnce(){ const player=window.gameClient?.player,world=window.gameClient?.world,pathfinder=world?.pathfinder; const candidates=[[player,"stopWalking"],[player,"stopAutoWalk"],[player,"cancelWalk"],[player,"cancelWalking"],[player,"clearPath"],[pathfinder,"stop"],[pathfinder,"cancel"],[pathfinder,"abort"],[pathfinder,"reset"],[pathfinder,"clearPath"],[world,"stopWalking"],[world,"cancelWalk"]]; for(const [target,method] of candidates){if(typeof target?.[method]!=="function")continue;try{target[method]();bot.log?.("GM safety forced walking stop",{method});return true;}catch(error){bot.log?.("GM safety walking stop command failed",{method,error:String(error)});}} const current=bot.getPlayerPosition?.(); if(current&&typeof pathfinder?.findPath==="function")try{const currentTile=typeof Position==="function"?new Position(Number(current.x),Number(current.y),Number(current.z)):current;pathfinder.findPath(current,currentTile);bot.log?.("GM safety forced walking stop",{method:"pathfinder.findPath(current,current)"});return true;}catch(error){bot.log?.("GM safety walking stop command failed",{method:"pathfinder.findPath(current,current)",error:String(error)});} bot.log?.("GM safety could not find walking stop command"); return false; }
  function triggerKillSwitch(source,speaker,message="",details={}){ if(!config.killSwitchEnabled)return false; bot.cave?.stop?.();bot.attack?.stop?.();const walkingStopped=forceStopWalkingOnce();bot.log?.("game master kill switch triggered",{source,speaker,message,walkingStopped,...details});bot.ui?.refreshAutoAttackStatus?.();bot.ui?.refreshCaveStatus?.();config.killSwitchEnabled=false;persistConfig();syncWatcher();refreshPanelControls();return true; }
  function resumeGmPause(){ if(!state.pauseActive)return false; const snapshot=state.pauseResumeSnapshot||{cave:false,attack:false}; state.pauseTimerId=null;state.pauseActive=false;state.pauseResumeSnapshot=null;if(snapshot.cave)bot.cave?.start?.();if(snapshot.attack)bot.attack?.start?.();bot.ui?.refreshAutoAttackStatus?.();bot.ui?.refreshCaveStatus?.();bot.log?.("GM pause resumed modules after 15 seconds",snapshot);return true; }
  function triggerGmPause(source,speaker,message="",details={}){ if(!config.pauseEnabled||state.pauseActive)return false; const snapshot={cave:!!bot.cave?.status?.().running,attack:!!bot.attack?.status?.().running};state.pauseActive=true;state.pauseResumeSnapshot=snapshot;if(snapshot.cave)bot.cave?.stop?.({persistEnabled:false});if(snapshot.attack)bot.attack?.stop?.({persistEnabled:false});const walkingStopped=forceStopWalkingOnce();bot.ui?.refreshAutoAttackStatus?.();bot.ui?.refreshCaveStatus?.();state.pauseTimerId=window.setTimeout(resumeGmPause,GM_PAUSE_MS);bot.log?.("GM pause triggered",{source,speaker,message,walkingStopped,pauseMs:GM_PAUSE_MS,resumeSnapshot:snapshot,...details});return true; }
  function handleGameMasterDetection(source,speaker,message="",details={}){ if(source==="visible-player"||source==="unknown-monster")triggerGmAlarm(source,speaker,details);const responderScheduled=(source==="chat"||source==="unknown-monster")?scheduleResponder(speaker,message):false;if(config.killSwitchEnabled)return triggerKillSwitch(source,speaker,message,{responderScheduled,...details});if(config.pauseEnabled){const paused=triggerGmPause(source,speaker,message,details);if(paused)return true;}if((source==="visible-player"||source==="unknown-monster")&&config.alarmEnabled)return true;if((source==="chat"||source==="unknown-monster")&&config.responderEnabled){bot.log?.("game master detected by responder",{speaker,message,responderScheduled,...details});return true;}return false; }
  function isConfiguredGameMaster(entry,speaker,gmNames){ if(speaker&&gmNames.has(normalizeName(speaker)))return true;return !!(entry?.isGameMaster||entry?.isGamemaster||entry?.gameMaster||entry?.gamemaster||entry?.speaker?.isGameMaster||entry?.sender?.isGameMaster||entry?.author?.isGameMaster); }
  function getVisibleConfiguredGameMaster(gmNames){ if(!gmNames.size)return null;const players=bot.xray?.getVisiblePlayers?.()||[];return players.find(player=>gmNames.has(normalizeName(player?.name)))||null; }
  function getUnlistedVisibleMonsters(){ if(!config.unknownMonsterEnabled)return[];const allowed=config.unknownMonsterAllowlist.map(normalizeName);return(bot.xray?.getVisibleMonsters?.({sameFloorOnly:true})||[]).filter(creature=>{if(creature?.type!==1)return false;const monsterName=normalizeName(creature?.name);return!allowed.some(allowedName=>monsterName.includes(allowedName));}); }
  function getMonsterKey(creature){ return String(creature?.id??`${normalizeName(creature?.name)}|${creature?.__position?.x??""}|${creature?.__position?.y??""}|${creature?.__position?.z??""}`); }
  function checkUnknownMonsters(){ if(!config.unknownMonsterEnabled){state.visibleUnknownMonsterKeys.clear();return false;}const unknown=getUnlistedVisibleMonsters(),currentKeys=new Set(unknown.map(getMonsterKey)),newMonster=unknown.find(creature=>!state.visibleUnknownMonsterKeys.has(getMonsterKey(creature)))||null;state.visibleUnknownMonsterKeys=currentKeys;if(!newMonster)return false;const position=newMonster?.getPosition?.()||newMonster?.__position||null;bot.log?.("unknown on-screen monster detected",{monsterName:newMonster?.name||null,monsterId:newMonster?.id??null,position});return handleGameMasterDetection("unknown-monster",newMonster?.name||"Unknown monster","Unlisted monster appeared",{monsterName:newMonster?.name||null,monsterId:newMonster?.id??null,position,allowlist:[...config.unknownMonsterAllowlist]}); }
  function tickNow(){ if(!state.watcherRunning||!shouldWatch())return false;if(state.timerId!=null)window.clearTimeout(state.timerId);state.timerId=null;tick();return true; }
  function tick(){ if(!state.watcherRunning||!shouldWatch())return;checkUnknownMonsters();if(!state.watcherRunning||!shouldWatch())return;const gmNames=new Set(getConfiguredGameMasterNames().map(normalizeName));if(config.killSwitchEnabled||config.pauseEnabled||config.alarmEnabled){const visibleGm=getVisibleConfiguredGameMaster(gmNames),visibleKey=visibleGm?normalizeName(visibleGm.name):null;if(!visibleGm)state.visibleGmKey=null;else if(visibleKey&&visibleKey!==state.visibleGmKey){state.visibleGmKey=visibleKey;handleGameMasterDetection("visible-player",visibleGm.name||"GM","",{position:visibleGm?.getPosition?.()||visibleGm?.__position||null});if(!state.watcherRunning||!shouldWatch())return;}}for(const item of getCurrentEntries()){if(state.seenEntryKeys.has(item.key))continue;state.seenEntryKeys.add(item.key);if(!item.speaker||!isConfiguredGameMaster(item.entry,item.speaker,gmNames))continue;handleGameMasterDetection("chat",item.speaker,item.message,{channel:item.channelName});if(!state.watcherRunning||!shouldWatch())return;}state.timerId=window.setTimeout(tick,1000); }
  function startWatcher(){ if(!shouldWatch()||state.watcherRunning)return false;state.watcherRunning=true;rememberExistingEntries();tick();return true; }
  function stopWatcher(){ state.watcherRunning=false;if(state.timerId!=null)window.clearTimeout(state.timerId);state.timerId=null;if(state.pendingReplyTimerId!=null)window.clearTimeout(state.pendingReplyTimerId);state.pendingReplyTimerId=null;state.responderPending=false;state.responderLockedUntil=0;state.visibleGmKey=null;state.visibleUnknownMonsterKeys.clear();state.seenEntryKeys.clear();return true; }
  function syncWatcher(){ if(shouldWatch())startWatcher();else stopWatcher(); }
  function ensurePanelControls(){ const panel=document.getElementById("minibia-bot-panel");if(!panel)return false;let killSwitchSection=document.getElementById("minibia-bot-gm-kill-switch-section");if(!killSwitchSection){const anchor=panel.querySelector("#minibia-bot-xray-section, #minibia-bot-player-screen-alarm-section, #minibia-bot-mining-section");killSwitchSection=document.createElement("div");killSwitchSection.id="minibia-bot-gm-kill-switch-section";killSwitchSection.className="mb-section mb-column-section";killSwitchSection.innerHTML=`<div class="mb-label">GM Kill Switch</div><div class="mb-stack"><label class="mb-toggle"><input type="checkbox" id="minibia-bot-gm-kill-switch-enabled" /><span>Enable GM Kill Switch</span></label><label class="mb-toggle"><input type="checkbox" id="minibia-bot-gm-pause-enabled" /><span>GM Pause</span></label><label class="mb-toggle"><input type="checkbox" id="minibia-bot-gm-alarm-enabled" /><span>GM Alarm</span></label><label class="mb-toggle"><input type="checkbox" id="minibia-bot-gm-responder-enabled" /><span>GM Auto Response</span></label><label>GM Response<input id="minibia-bot-gm-responder-message" type="text" placeholder="Message to send" /></label><div class="mb-small-note">Kill switch: stop walking + stop Cavebot/Auto Attack. GM Pause: stop walking + pause Cavebot/Auto Attack for 15s. GM Alarm: when an exact GM name is detected on screen, say \"god on screen\" for 10s. Auto Response: reply once after 2s, then reset after 30s. Exact-name matches work for visible players and chat/private messages.</div></div>`;if(anchor?.parentNode)anchor.parentNode.insertBefore(killSwitchSection,anchor.nextSibling);else(panel.querySelector(".mb-side-column")||panel.querySelector(".mb-main-column")||panel).appendChild(killSwitchSection);}const killToggle=document.getElementById("minibia-bot-gm-kill-switch-enabled"),pauseToggle=document.getElementById("minibia-bot-gm-pause-enabled"),alarmToggle=document.getElementById("minibia-bot-gm-alarm-enabled"),responderToggle=document.getElementById("minibia-bot-gm-responder-enabled"),responderMessage=document.getElementById("minibia-bot-gm-responder-message"),exactNamesInput=document.getElementById("minibia-bot-gm-exact-names");if(killToggle&&!killToggle.dataset.gmKillBound){killToggle.dataset.gmKillBound="1";killToggle.addEventListener("change",()=>{config.killSwitchEnabled=!!killToggle.checked;persistConfig();syncWatcher();refreshPanelControls();});}if(pauseToggle&&!pauseToggle.dataset.gmPauseBound){pauseToggle.dataset.gmPauseBound="1";pauseToggle.addEventListener("change",()=>{config.pauseEnabled=!!pauseToggle.checked;persistConfig();syncWatcher();refreshPanelControls();});}if(alarmToggle&&!alarmToggle.dataset.gmAlarmBound){alarmToggle.dataset.gmAlarmBound="1";alarmToggle.addEventListener("change",()=>{config.alarmEnabled=!!alarmToggle.checked;if(!config.alarmEnabled)stopGmAlarmSpeech();persistConfig();syncWatcher();refreshPanelControls();});}if(responderToggle&&!responderToggle.dataset.gmResponderBound){responderToggle.dataset.gmResponderBound="1";responderToggle.addEventListener("change",()=>{config.responderEnabled=!!responderToggle.checked;persistConfig();syncWatcher();refreshPanelControls();});}if(responderMessage&&!responderMessage.dataset.gmResponderMessageBound){responderMessage.dataset.gmResponderMessageBound="1";const saveResponderMessage=()=>{config.responderMessage=String(responderMessage.value||"").trim();persistConfig();};responderMessage.addEventListener("change",saveResponderMessage);responderMessage.addEventListener("blur",saveResponderMessage);}if(exactNamesInput&&!exactNamesInput.dataset.gmNamesBound){exactNamesInput.dataset.gmNamesBound="1";const saveNames=()=>writeExactNames(exactNamesInput.value);exactNamesInput.addEventListener("change",saveNames);exactNamesInput.addEventListener("blur",saveNames);}refreshPanelControls();return true; }
  function ensureUnknownMonsterControls(){
    const panel=document.getElementById("minibia-bot-panel");
    const section=document.getElementById("minibia-bot-gm-kill-switch-section");
    if(!panel||!section)return false;
    if(!panel.contains(section)){
      const column=panel.querySelector(".mb-side-column")||panel.querySelector(".mb-main-column")||panel.querySelector(".mb-body")||panel;
      column.appendChild(section);
    }
    let controls=document.getElementById("minibia-bot-gm-unknown-monster-controls");
    if(!controls){
      controls=document.createElement("div");controls.id="minibia-bot-gm-unknown-monster-controls";controls.className="mb-section mb-column-section";
      controls.innerHTML=`<div class="mb-label">GM Monster Responder</div><div class="mb-stack"><label class="mb-toggle"><input type="checkbox" id="minibia-bot-gm-unknown-monster-enabled" /><span>Enable GM Monster Responder</span></label><div class="mb-inline"><input type="text" id="minibia-bot-gm-unknown-monster-input" placeholder="Allowed monster name" /><button type="button" class="mb-small-button" id="minibia-bot-gm-unknown-monster-add">Add</button></div><div class="mb-list" id="minibia-bot-gm-unknown-monster-list"></div><div class="mb-small-note">Allowlist entries match any part of a same-floor on-screen monster name. Monsters with no partial match trigger the enabled GM actions immediately. Players and NPCs are ignored.</div></div>`;
    }
    if(controls.parentNode!==section.parentNode||controls.previousElementSibling!==section)section.parentNode.insertBefore(controls,section.nextSibling);
    const toggle=document.getElementById("minibia-bot-gm-unknown-monster-enabled"),input=document.getElementById("minibia-bot-gm-unknown-monster-input"),addButton=document.getElementById("minibia-bot-gm-unknown-monster-add");
    if(toggle&&!toggle.dataset.gmUnknownMonsterBound){toggle.dataset.gmUnknownMonsterBound="1";toggle.addEventListener("change",()=>{config.unknownMonsterEnabled=!!toggle.checked;state.visibleUnknownMonsterKeys.clear();persistConfig();syncWatcher();refreshPanelControls();tickNow();});}
    const addAllowedMonster=()=>{const name=String(input?.value||"").trim();if(!name||config.unknownMonsterAllowlist.some(item=>normalizeName(item)===normalizeName(name)))return false;config.unknownMonsterAllowlist.push(name);config.unknownMonsterAllowlist=normalizeNameList(config.unknownMonsterAllowlist).sort((a,b)=>a.localeCompare(b));if(input)input.value="";persistConfig();state.visibleUnknownMonsterKeys.clear();refreshPanelControls();tickNow();return true;};
    if(addButton&&!addButton.dataset.gmUnknownMonsterAddBound){addButton.dataset.gmUnknownMonsterAddBound="1";addButton.addEventListener("click",addAllowedMonster);}
    if(input&&!input.dataset.gmUnknownMonsterInputBound){input.dataset.gmUnknownMonsterInputBound="1";input.addEventListener("keydown",event=>{if(event.key!=="Enter")return;event.preventDefault();addAllowedMonster();});}
    refreshPanelControls();return true;
  }
  function startPanelWatcher(){ if(state.panelTimerId!=null)return;ensurePanelControls();ensureUnknownMonsterControls();state.panelTimerId=window.setInterval(()=>{ensurePanelControls();ensureUnknownMonsterControls();},1000); }
  function stopPanelWatcher(){ if(state.panelTimerId!=null)window.clearInterval(state.panelTimerId);state.panelTimerId=null; }
  function destroy(){ stopWatcher();stopPanelWatcher();stopGmAlarmSpeech();document.getElementById("minibia-bot-gm-unknown-monster-controls")?.remove();if(state.pauseTimerId!=null)window.clearTimeout(state.pauseTimerId);state.pauseTimerId=null;state.pauseActive=false;state.pauseResumeSnapshot=null; }
  bot.gmKillSwitch={get config(){return {...config,unknownMonsterAllowlist:[...config.unknownMonsterAllowlist]};},get exactNames(){return readExactNames();},get unknownMonsterAllowlist(){return[...config.unknownMonsterAllowlist];},setExactNames:writeExactNames,setUnknownMonsterAllowlist,startWatcher,stopWatcher,syncWatcher,triggerKillSwitch,triggerGmPause,triggerGmAlarm,forceStopWalkingOnce,destroy};
  bot.addCleanup?.(destroy);persistConfig();startPanelWatcher();syncWatcher();return bot.gmKillSwitch;
};
